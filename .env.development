# 개발 환경 설정
# Rust 백엔드(Tauri)의 API URL 은 아래 파일에서 관리합니다.
#   → src-tauri/.cargo/config.toml  (API_BASE_URL_DEV, API_BASE_URL_PROD)
#
# Vite 프론트엔드용 환경변수가 필요하면 VITE_ 접두사를 사용하세요.
# 예) VITE_APP_TITLE=POS Dev
