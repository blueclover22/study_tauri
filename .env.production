# 운영 환경 설정
# Rust 백엔드(Tauri)의 API URL 은 아래 파일에서 관리합니다.
#   → src-tauri/.cargo/config.toml  (API_BASE_URL_PROD)
# CI/CD 에서 환경변수를 주입한다면 config.toml 의 값보다 CI 값이 우선됩니다.
#
# Vite 프론트엔드용 환경변수가 필요하면 VITE_ 접두사를 사용하세요.
# 예) VITE_APP_TITLE=POS
